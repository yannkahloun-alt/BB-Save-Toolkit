[cosmic-ray]
module-path = "bbtool/projection/progression.py"
timeout = 20.0
excluded-modules = []
test-command = "python -m pytest -c tests/pytest.ini -o cache_dir=tests/cache/pytest -q -x tests/unit/test_progression.py"

[cosmic-ray.distributor]
name = "local"
